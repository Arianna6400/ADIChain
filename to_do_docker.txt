Comandi da eseguire su terminale

BUILD DI DOCKER:

docker build -t adichain .

RUN DEL CONTAINER:

docker run -it -p 8000:8000 adichain

DOCKER-COMPOSE:

docker compose up -d
docker compose run -it adichain

SCRIPT:
chmod +x ./extract.sh
./extract.sh

